spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/archive
        username: postgres
        password: root
    jpa:
        generate-ddl: true
        hibernate:
            ddl-auto: update
        show-sql: false
logging:
    level:
        org.hibernate:
            SQL: debug
            type: trace

server:
    port: 3000